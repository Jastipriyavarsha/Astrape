{
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ],
  "env": {
    "REACT_APP_API_URL": "https://your-backend.onrender.com/api"
  },
  "ignoreCommand": "echo Using .gitignore",
  "github": {
    "silent": true
  }
}
